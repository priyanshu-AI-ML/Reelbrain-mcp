# MCP server
mcp[cli]>=1.2.0

# HTTP client
httpx>=0.27.0

# Instagram DM polling
instaloader>=4.10.3

# Reel download (yt-dlp is also installed as CLI via pip)
yt-dlp>=2024.1.0

# Transcription (CPU, int8 quantized)
faster-whisper>=1.0.3

# Vector search
chromadb>=0.5.0

# Gemini calls via httpx (already listed above, no extra SDK needed)
